apiVersion: v1
kind: Service
metadata:
  name: piafadex-backend
  namespace: piafadex
spec:
  type: ClusterIP
  selector:
    app: piafadex-backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
